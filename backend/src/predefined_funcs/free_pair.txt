_freepair:
	pushq %rbp
	movq %rsp, %rbp
	# external calls must be stack-aligned to 16 bytes, accomplished by masking with fffffffffffffff0
	andq $-16, %rsp
	cmpq $0, %rdi
	je _errNull
	call free@plt
	movq %rbp, %rsp
	popq %rbp
	ret

